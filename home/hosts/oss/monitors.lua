-- placeholder: every output at its preferred mode, laid out automatically
-- run nwg-displays on the machine to write the real layout back into this file

hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = 1.0
})
